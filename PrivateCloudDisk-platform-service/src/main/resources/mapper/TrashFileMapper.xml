<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.project.mapper.TrashFileMapper">
    
    <resultMap id="TrashFileEntityMap" type="org.project.model.entity.TrashFileEntity">
        <id column="trash_id" property="trash_id"/>
        <result column="trash_file_id" property="file_id"/>
        <result column="trash_user_id" property="user_id"/>
        <result column="trash_file_name" property="file_name"/>
        <result column="trash_file_type" property="file_type"/>
        <result column="trash_file_size" property="file_size"/>
        <result column="trash_original_node_id" property="original_node_id"/>
        <result column="trash_storage_path" property="storage_path"/>
        <result column="trash_file_checksum" property="file_checksum"/>
        <result column="trash_deleted_at" property="deleted_at"/>
        <result column="trash_expires_at" property="expires_at"/>
    </resultMap>
    
    <insert id="insertTrashFile" parameterType="org.project.model.entity.TrashFileEntity" useGeneratedKeys="true" keyProperty="trash_id">
        INSERT INTO pcd_trash_file_table (
            trash_file_id, trash_user_id, trash_file_name, trash_file_type, 
            trash_file_size, trash_original_node_id, trash_storage_path, 
            trash_file_checksum, trash_deleted_at, trash_expires_at
        ) VALUES (
            #{file_id}, #{user_id}, #{file_name}, #{file_type}, 
            #{file_size}, #{original_node_id}, #{storage_path}, 
            #{file_checksum}, #{deleted_at}, #{expires_at}
        )
    </insert>
    
    <delete id="deleteTrashFile">
        DELETE FROM pcd_trash_file_table 
        WHERE trash_id = #{trash_id} AND trash_user_id = #{user_id}
    </delete>
    
    <select id="findTrashFileById" resultMap="TrashFileEntityMap">
        SELECT * FROM pcd_trash_file_table 
        WHERE trash_id = #{trash_id} AND trash_user_id = #{user_id}
    </select>
    
    <select id="findTrashFilesByUserId" resultMap="TrashFileEntityMap">
        SELECT * FROM pcd_trash_file_table 
        WHERE trash_user_id = #{user_id}
        ORDER BY trash_deleted_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    
    <select id="countTrashFilesByUserId" resultType="int">
        SELECT COUNT(*) FROM pcd_trash_file_table 
        WHERE trash_user_id = #{user_id}
    </select>
    
    <select id="findExpiredTrashFiles" resultMap="TrashFileEntityMap">
        SELECT * FROM pcd_trash_file_table 
        WHERE trash_expires_at &lt; NOW()
    </select>
    
    <select id="findTrashFileByFileId" resultMap="TrashFileEntityMap">
        SELECT * FROM pcd_trash_file_table 
        WHERE trash_file_id = #{file_id} AND trash_user_id = #{user_id}
    </select>
    
</mapper>
