<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.project.im.platform.mapper.ImConversationMapper">
    <resultMap id="BaseResultMap" type="org.project.im.platform.entity.ImConversation">
        <id column="id" property="id"/><result column="session_id" property="sessionId"/>
        <result column="session_type" property="sessionType"/><result column="user_id" property="userId"/>
        <result column="peer_id" property="peerId"/><result column="is_pinned" property="isPinned"/>
        <result column="is_muted" property="isMuted"/><result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    <sql id="BaseColumns">session_id, session_type, user_id, peer_id, is_pinned, is_muted, created_at, updated_at</sql>
    <insert id="insert" parameterType="org.project.im.platform.entity.ImConversation">INSERT INTO pcd_im_conversation (<include refid="BaseColumns"/>) VALUES (#{sessionId},#{sessionType},#{userId},#{peerId},#{isPinned},#{isMuted},#{createdAt},#{updatedAt})</insert>
    <select id="selectBySessionIdAndUserId" resultMap="BaseResultMap">SELECT id,<include refid="BaseColumns"/> FROM pcd_im_conversation WHERE session_id=#{sessionId} AND user_id=#{userId}</select>
    <select id="selectByUserIdAndPeerId" resultMap="BaseResultMap">SELECT id,<include refid="BaseColumns"/> FROM pcd_im_conversation WHERE user_id=#{userId} AND peer_id=#{peerId} AND session_type=#{sessionType}</select>
    <select id="selectByUserId" resultMap="BaseResultMap">SELECT id,<include refid="BaseColumns"/> FROM pcd_im_conversation WHERE user_id=#{userId} ORDER BY updated_at DESC</select>
    <update id="updatePinned">UPDATE pcd_im_conversation SET is_pinned=#{isPinned},updated_at=NOW() WHERE session_id=#{sessionId} AND user_id=#{userId}</update>
    <update id="updateMuted">UPDATE pcd_im_conversation SET is_muted=#{isMuted},updated_at=NOW() WHERE session_id=#{sessionId} AND user_id=#{userId}</update>
</mapper>
