<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.project.billing.mapper.UserCouponMapper">

    <resultMap id="UserCouponMap" type="org.project.billing.model.entity.UserCouponEntity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="coupon_id" property="couponId"/>
        <result column="coupon_code" property="couponCode"/>
        <result column="status" property="status"/>
        <result column="order_id" property="orderId"/>
        <result column="used_at" property="usedAt"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="org.project.billing.model.entity.UserCouponEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pcd_user_coupon_table (user_id, coupon_id, coupon_code, status)
        VALUES (#{userId}, #{couponId}, #{couponCode}, #{status})
    </insert>

    <update id="useCoupon">
        UPDATE pcd_user_coupon_table
        SET status = 'USED', order_id = #{orderId}, used_at = #{usedAt}
        WHERE id = #{id}
    </update>

    <select id="findByUserId" resultMap="UserCouponMap">
        SELECT * FROM pcd_user_coupon_table WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="findByUserIdAndCouponId" resultMap="UserCouponMap">
        SELECT * FROM pcd_user_coupon_table WHERE user_id = #{userId} AND coupon_id = #{couponId} AND status = 'UNUSED' LIMIT 1
    </select>

    <select id="countByUserIdAndCouponId" resultType="int">
        SELECT COUNT(*) FROM pcd_user_coupon_table WHERE user_id = #{userId} AND coupon_id = #{couponId}
    </select>

</mapper>